EnglishHelper 🚀
================

> ## 🤖 About Development / О разработке
> This project is a pure experiment in AI-driven coding. Every line of code in Python, the logic behind the global hooks, and the GUI implementation were generated by a Large Language Model based on high-level prompts.
> Этот проект — эксперимент в области кодинга с помощью ИИ. Каждая строчка кода на Python, логика глобальных хуков и реализация интерфейса были сгенерированы Большой Языковой Моделью на основе запросов пользователя.

# Описание

**EnglishHelper**  — это умный компаньон для тех, кто практикует слепую печать на английском языке и хочет одновременно расширять свой словарный запас.

Программа работает в фоне и анализирует все, что вы печатаете на клавиатуре в любом приложении (будь то тренажер печати, текстовый редактор или браузер). Как только вы дописываете слово,  **EnglishHelper**  мгновенно показывает его перевод, транскрипцию и визуальную ассоциацию (картинку) в ненавязчивом виджете поверх всех окон.

Для кого это приложение?
------------------------

*   ⌨️ **Для тех, кто учится слепой печати:** Вы механически набиваете текст, а приложение наполняет этот процесс смыслом, показывая значения незнакомых слов без отрыва от тренировки.
    
*   🇬🇧 **Для изучающих английский:** Контекстный перевод помогает запоминать слова «на лету», связывая моторику рук, визуальный образ и аудиальное восприятие.
    
*   📚 **Для читателей и писателей:** Мгновенный доступ к синонимам, определениям и переводу предложений без необходимости переключаться в браузер и гуглить.
    

Ключевая фишка
--------------

Вам не нужно копировать и вставлять текст. **Просто печатайте.** Приложение само поймет, когда вы закончили слово, отфильтрует слишком простые слова (например, _the_, _is_, _a_), чтобы не отвлекать вас, и покажет карточку только для действительно интересной лексики.

Вот полный список функционала **EnglishHelper**, который мы реализовали на данный момент. Это фактически ТЗ (Техническое Задание) того, что уже есть в коде.

1\. Глобальный перехват и анализ ввода
--------------------------------------

*   **Глобальный хук клавиатуры:** Приложение работает в фоне и слушает нажатия клавиш в любой программе.
    
*   **Определение раскладки:** Реагирует только на английскую раскладку клавиатуры.
    
*   **Игнорирование спецкоманд:** Не мешает работе системных шорткатов (Ctrl+..., Alt+...).
    
*   **Буфер ввода (Виртуальный редактор):**
    
    *   Собирает буквы в слова.
        
    *   Понимает Backspace, Delete и стрелки для коррекции написанного слова "на лету".
        
    *   Определяет окончание слова (пробел, знаки препинания) для запуска перевода.
        
*   **Интеграция с буфером обмена:**
    
    *   Нажатие Ctrl+C с выделенным словом автоматически запускает поиск этого слова в приложении.
        

2\. Главное окно (Карточка слова)
---------------------------------

*   **Безрамочный дизайн:** Окно не имеет системных рамок, всегда поверх других окон (Always on Top).
    
*   **Умное позиционирование:** Запоминает свою позицию и размер при перезапуске.
    
*   **Адаптивный интерфейс:**
    
    *   Скроллируемая область контента (если определений много).
        
    *   Grip (уголок) для изменения размера окна.
        
*   **Контент карточки:**
    
    *   **Слово:** Крупный заголовок.
        
    *   **Транскрипция:** Фонетическое написание.
        
    *   **Перевод:** Основной русский перевод (Яндекс/Словарь).
        
    *   **Изображение:** Автоматическая подгрузка картинки (приоритет Pexels -> Wikipedia). Фильтрация "мусорных" логотипов Википедии.
        
    *   **Детали:** Части речи, определения (на английском), примеры использования.
        
    *   **Синонимы:** Выводятся в виде кликабельных тегов. Клик по синониму запускает поиск этого синонима.
        
*   **Фильтрация простых слов (Vocabulary Level):**
    
    *   Слайдер (0-100) внизу окна.
        
    *   Позволяет игнорировать слишком простые/частые слова (чтобы не отвлекаться на "the", "and", "is").
        
    *   Показывает всплывающее окно (Popup) со списком слов текущего уровня при нажатии на слайдер.
        

3\. Окно перевода предложений (Sentence Window)
-----------------------------------------------

*   **Отдельное плавающее окно:** Также безрамочное, всегда сверху.
    
*   **Синхронный перевод:**
    
    *   Верхняя строка: отображает то, что вы печатаете в данный момент (на английском).
        
    *   Нижняя строка: показывает автоматический перевод предложения на русский (через Google Translate).
        
*   **Оптимизация запросов:** Перевод запрашивается только при завершении слов или пауз, чтобы не спамить API.
    
*   **Единый стиль:** Шрифт перевода совпадает по стилю с главным окном.
    
*   **Управление видимостью:** Включается/выключается кнопкой в главном окне.
    

4\. Мультимедиа и Аудио
-----------------------

*   **Озвучка (Text-to-Speech / MP3):**
    
    *   Скачивание реальных произношений (не робот) из словаря.
        
    *   Кнопки ручного воспроизведения: **US** (Америка) и **UK** (Британия).
        
*   **Автопроизношение:**
    
    *   Опция автоматического проигрывания аудио при нахождении слова.
        
    *   Приоритет: Американское произношение -> Любое не британское -> Любое доступное.
        
    *   Управление отдельной кнопкой "Pronunciation" внизу главного окна.
        

5\. Вспомогательные функции (Tools)
-----------------------------------

*   **Мгновенный перевод при наведении:** Если навести мышку на английское определение или пример в карточке, всплывает Tooltip с русским переводом этого текста.
    
*   **Кэширование:**
    
    *   Все найденные слова и их данные сохраняются локально (Data/Cache/).
        
    *   Изображения кэшируются (Data/Images/).
        
    *   При повторном вводе данные берутся из кэша мгновенно, без запросов к интернету.
        
*   **Статус-бар:** Отображает источник перевода (API/Cache) и источник картинки (Pexels/Wiki).
    

6\. Настройки и Управление
--------------------------

*   **Settings.ini:** Хранение API ключей (Yandex, Pexels) и геометрии окон.
    
*   **Управление из UI:**
    
    *   Кнопка **"Sentence"**: Вкл/Выкл окно предложений (индикатор цветом).
        
    *   Кнопка **"Pronunciation"**: Вкл/Выкл автопроизношение (индикатор цветом).
        
    *   Слайдер уровня сложности.
        
*   **Отсутствие отдельного окна настроек:** Для простоты интерфейса все важные переключатели вынесены в главный бар, а ключи API задаются один раз в файле.
    

7\. Техническая реализация
--------------------------

*   **Многопоточность:** API запросы (перевод, картинки, аудио) идут в отдельных потоках, не замораживая интерфейс.
    
*   **Запуск (Run.bat):**
    
    *   Автоматическое создание виртуального окружения (.venv).
        
    *   Установка зависимостей.
        
    *   Тихий запуск без консольного окна (pythonw).

## 📂 Структура каталогов (Project Structure)
Проект организован по модульному принципу для разделения логики (сеть, данные, GUI).
```
EnglishHelper/               <-- Корневая директория проекта
│
├── Data/                    <-- Папка для хранения локальных данных
│   ├── Dict/                <-- Кэш JSON-ответов от словарей (чтобы не спамить API)
│   ├── Images/              <-- Кэш изображений с Pexels
│   └── vocab_10k.txt        <-- Файл со списком 10,000 частых слов (скачивается автоматически)
│
├── gui/                     <-- Пакет, содержащий все файлы интерфейса (Tkinter)
│   ├── __init__.py          <-- Маркер пакета (пустой)
│   ├── main_window.py       <-- Основное окно (карточка слова, картинка, слайдер)
│   ├── sent_window.py       <-- Окно перевода предложений (появляется при наборе текста)
│   ├── popup.py             <-- Всплывающее окно со списком слов для текущего уровня
│   └── styles.py            <-- Константы стилей (цвета, шрифты) для легкой смены темы
│
├── .gitignore               <-- Список исключений для Git (settings.ini, venv, кэши)
├── config.py                <-- Менеджер конфигурации (работа с .ini файлом)
├── editor.py                <-- Логика "виртуального текстового редактора" (буфер ввода)
├── main.pyw                 <-- ТОЧКА ВХОДА. Запускает клавиатурный хук и GUI
├── network.py               <-- Все сетевые запросы (Yandex, Pexels, Google Translate)
├── settings.ini             <-- Файл настроек пользователя и API ключей (в Git не добавлять!)
├── settings.ini.example     <-- Шаблон настроек для репозитория
└── vocab.py                 <-- Загрузка словаря и фильтрация слов (лемматизация)
```

## 📄 Справочник файлов

### 1. Основные модули (Core)

| Файл             | Описание                                                                                                                                                                                                                                             |
|:-----------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **`main.pyw`**   | **Точка входа.** Инициализирует приложение и устанавливает глобальный перехватчик исключений. Содержит `keyboard.hook` — слушатель, который перехватывает нажатия клавиш глобально, передает их в `editor` и запускает потоки обновления интерфейса. |
| **`config.py`**  | **Менеджер конфигурации.** Отвечает за чтение/запись файла `settings.ini`. Определяет абсолютные пути к ресурсам (`Data`, `Dict`), обеспечивая совместимость как при запуске скрипта, так и в скомпилированном `.exe`.                               |
| **`network.py`** | **API Клиент.** Содержит функции: `fetch_full_data` (Словарь + Перевод), `fetch_image` (Pexels) и `fetch_sentence_translation` (Google). Реализует логику кэширования: сначала ищет локальный JSON, затем делает запрос к API.                       |
| **`vocab.py`**   | **Логика словаря.** Скачивает частотный список Google 10k. Реализует функцию `is_word_too_simple`, используя `lemminflect` для нормализации слов (приведения к начальной форме) и проверки их ранга частотности.                                     |
| **`editor.py`**  | **Текстовый буфер.** Содержит класс `TextEditorSimulator`. Эмулирует текстовое поле (движение курсора, вставка, удаление) в памяти, чтобы отслеживать, что пользователь печатает глобально, без фокуса на окне приложения.                           |

### 2. Модули интерфейса (пакет `gui/`)

| Файл                 | Описание                                                                                                                                                                                                                     |
|:---------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **`styles.py`**      | **Дизайн-система.** Содержит словари `COLORS` (палитра темной темы) и `FONTS`. Все визуальные правки стилей должны вноситься здесь.                                                                                          |
| **`main_window.py`** | **Основной UI.** Класс `MainWindow`. Управляет вертикальным окном "Карточки" (Информация о слове через виджет Text с тегами, холст Картинки, Слайдер словаря). Управляет потоками обновления данных (`update_ui_with_data`). |
| **`sent_window.py`** | **UI Перевода.** Класс `SentenceWindow`. Горизонтальное окно-оверлей, показывающее набираемый текст (Курсив) и перевод (Жирный) в реальном времени. Содержит методы `show/hide`, управляемые настройками.                    |
| **`popup.py`**       | **UI Списка.** Вспомогательное окно, отображающее список слов, соответствующих текущему уровню фильтра словаря (вызывается кликом по слайдеру).                                                                              |

### 3. Конфигурация и Данные

| Файл                       | Описание                                                                                                                         |
|:---------------------------|:---------------------------------------------------------------------------------------------------------------------------------|
| **`settings.ini.example`** | **Шаблон.** Файл-пример с пустыми полями для ключей. Разработчики должны переименовать его в `settings.ini` и ввести свои ключи. |

 
## 🛠 Зависимости

**Версия Python:** 3.10+

**Необходимые библиотеки:**
Установка через `pip install -r requirements.txt` (если создан) или вручную:
pip install requests pillow keyboard lemminflect pyttsx3 pyinstaller


*   `requests`: HTTP-клиент для работы с API.
*   `keyboard`: Глобальный перехватчик клавиатуры.
*   `pillow`: Обработка изображений для Tkinter.
*   `lemminflect`: NLP лемматизация (поиск корней слов).
*   `pyttsx3`: Офлайн синтез речи (Text-to-Speech).
*   `pyinstaller`: (Опционально) Для компиляции в `.exe`.

**Внешние API (Требуются ключи):**
1.  **Yandex Dictionary API**: Для перевода отдельных слов.
2.  **Pexels API**: Для поиска изображений.
3.  **Google Translate (GTX)**: Используется через неофициальный endpoint (ключ не требуется) для перевода предложений.

