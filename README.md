# EnglishHelper

## 📂 Структура каталогов (Project Structure)
Проект организован по модульному принципу для разделения логики (сеть, данные, GUI).
```
EnglishHelper/               <-- Корневая директория проекта
│
├── Data/                    <-- Папка для хранения локальных данных
│   ├── Dict/                <-- Кэш JSON-ответов от словарей (чтобы не спамить API)
│   ├── Images/              <-- Кэш изображений с Pexels
│   └── vocab_10k.txt        <-- Файл со списком 10,000 частых слов (скачивается автоматически)
│
├── gui/                     <-- Пакет, содержащий все файлы интерфейса (Tkinter)
│   ├── __init__.py          <-- Маркер пакета (пустой)
│   ├── main_window.py       <-- Основное окно (карточка слова, картинка, слайдер)
│   ├── sent_window.py       <-- Окно перевода предложений (появляется при наборе текста)
│   ├── popup.py             <-- Всплывающее окно со списком слов для текущего уровня
│   └── styles.py            <-- Константы стилей (цвета, шрифты) для легкой смены темы
│
├── .gitignore               <-- Список исключений для Git (settings.ini, venv, кэши)
├── config.py                <-- Менеджер конфигурации (работа с .ini файлом)
├── editor.py                <-- Логика "виртуального текстового редактора" (буфер ввода)
├── main.pyw                 <-- ТОЧКА ВХОДА. Запускает клавиатурный хук и GUI
├── network.py               <-- Все сетевые запросы (Yandex, Pexels, Google Translate)
├── settings.ini             <-- Файл настроек пользователя и API ключей (в Git не добавлять!)
├── settings.ini.example     <-- Шаблон настроек для репозитория
└── vocab.py                 <-- Загрузка словаря и фильтрация слов (лемматизация)
```

## 📄 Справочник файлов

### 1. Основные модули (Core)

| Файл | Описание |
| :--- | :--- |
| **`main.pyw`** | **Точка входа.** Инициализирует приложение и устанавливает глобальный перехватчик исключений. Содержит `keyboard.hook` — слушатель, который перехватывает нажатия клавиш глобально, передает их в `editor` и запускает потоки обновления интерфейса. |
| **`config.py`** | **Менеджер конфигурации.** Отвечает за чтение/запись файла `settings.ini`. Определяет абсолютные пути к ресурсам (`Data`, `Dict`), обеспечивая совместимость как при запуске скрипта, так и в скомпилированном `.exe`. |
| **`network.py`** | **API Клиент.** Содержит функции: `fetch_full_data` (Словарь + Перевод), `fetch_image` (Pexels) и `fetch_sentence_translation` (Google). Реализует логику кэширования: сначала ищет локальный JSON, затем делает запрос к API. |
| **`vocab.py`** | **Логика словаря.** Скачивает частотный список Google 10k. Реализует функцию `is_word_too_simple`, используя `lemminflect` для нормализации слов (приведения к начальной форме) и проверки их ранга частотности. |
| **`editor.py`** | **Текстовый буфер.** Содержит класс `TextEditorSimulator`. Эмулирует текстовое поле (движение курсора, вставка, удаление) в памяти, чтобы отслеживать, что пользователь печатает глобально, без фокуса на окне приложения. |

### 2. Модули интерфейса (пакет `gui/`)

| Файл | Описание |
| :--- | :--- |
| **`styles.py`** | **Дизайн-система.** Содержит словари `COLORS` (палитра темной темы) и `FONTS`. Все визуальные правки стилей должны вноситься здесь. |
| **`main_window.py`** | **Основной UI.** Класс `MainWindow`. Управляет вертикальным окном "Карточки" (Информация о слове через виджет Text с тегами, холст Картинки, Слайдер словаря). Управляет потоками обновления данных (`update_ui_with_data`). |
| **`sent_window.py`** | **UI Перевода.** Класс `SentenceWindow`. Горизонтальное окно-оверлей, показывающее набираемый текст (Курсив) и перевод (Жирный) в реальном времени. Содержит методы `show/hide`, управляемые настройками. |
| **`popup.py`** | **UI Списка.** Вспомогательное окно, отображающее список слов, соответствующих текущему уровню фильтра словаря (вызывается кликом по слайдеру). |

### 3. Конфигурация и Данные

| Файл | Описание |
| :--- | :--- |
| **`settings.ini.example`** | **Шаблон.** Файл-пример с пустыми полями для ключей. Разработчики должны переименовать его в `settings.ini` и ввести свои ключи. |

 
## 🛠 Зависимости

**Версия Python:** 3.10+

**Необходимые библиотеки:**
Установка через `pip install -r requirements.txt` (если создан) или вручную:
pip install requests pillow keyboard lemminflect pyttsx3 pyinstaller


*   `requests`: HTTP-клиент для работы с API.
*   `keyboard`: Глобальный перехватчик клавиатуры.
*   `pillow`: Обработка изображений для Tkinter.
*   `lemminflect`: NLP лемматизация (поиск корней слов).
*   `pyttsx3`: Офлайн синтез речи (Text-to-Speech).
*   `pyinstaller`: (Опционально) Для компиляции в `.exe`.

**Внешние API (Требуются ключи):**
1.  **Yandex Dictionary API**: Для перевода отдельных слов.
2.  **Pexels API**: Для поиска изображений.
3.  **Google Translate (GTX)**: Используется через неофициальный endpoint (ключ не требуется) для перевода предложений.

